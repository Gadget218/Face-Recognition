The python script separates the images in a dataset folder containing the faces of people in an input folder named "people".    
Libraries used: cv2(opencv), pickle, numpy, face_recognition
#Work In progress
